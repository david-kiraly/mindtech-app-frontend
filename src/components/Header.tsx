import React from 'react';
import { Col } from 'react-bootstrap';

const Header = () => {
    return (
        <Col className="text-white bg-secondary">
            <h1 className="text-center">Hungarian Covid Data</h1>
        </Col>
    );
};

export default Header;